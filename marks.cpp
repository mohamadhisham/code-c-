# include <iostream>
using namespace std;
int main()
{
	int mark;
	cin>>mark;
	if (mark>=90)
		cout<<"a";
	else if (mark>=80)
		cout<<"b";
	else if (mark>=70)
		cout<<"c";
	else if (mark>=60)
		cout<<"d";
	else
		cout<<"f";
return 0;
}
